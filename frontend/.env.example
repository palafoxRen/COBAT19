# URL base del API. Con same-origin (proxied por vercel.json) basta con /api.
VITE_API_URL=/api

# Configuracion de Supabase Storage (anon key es publico por diseno)
VITE_SUPABASE_URL=https://<referencia>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon_key_jwt>
VITE_SUPABASE_BUCKET=uploads
